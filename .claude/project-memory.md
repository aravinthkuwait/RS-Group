# RS Group Project Memory

- 2026-07-02: Built full system (Express+React+Expo), SQLite first, 56-test e2e suite.
- 2026-07-02: Migrated to Supabase Postgres, schema rsgroup, RLS deny-by-default.
- 2026-07-03: Created dedicated Supabase project 'RS Group Medical Shop' (ap-south-1); removed pharmacy schema from Laundry Spot project — zero overlap.
- 2026-07-03: Deployed to Railway (rs-group-production.up.railway.app, PORT=8080); auto-seed on first boot worked.
- 2026-07-03: Fixed blank-page (stale asset cache -> 404 + cache headers) and tab-switch crash (16x useEffect promise-cleanup bug). Added error boundary.
- 2026-07-03: Added GitHub Actions CI (build + Postgres + e2e); run #1 green.
- 2026-07-03: User requested always-on skills: caveman (exists), memory (created in-repo); graphify/headroom/ponytail not found anywhere.
- 2026-07-03: Added PWA install (manifest+icons+sw) so staff install app from rs-group-production.up.railway.app; native APK needs Expo/EAS account, offered CI-built APK.
- 2026-07-03: Prepped mobile/ for EAS: SDK52, expo-camera, eas.json (preview=APK), BASE_URL=Railway. Waiting on user's EAS projectId to replace placeholder in app.json.
- 2026-07-03: EAS wired: project rs-group (owner aravinthkuwait, id 10c7a4a8-fd02-4bfb-9cba-aef7b84f579b), GitHub repo connected on expo.dev. User builds APK via Builds -> Build from GitHub, branch claude/rs-group-medical-saas-k2eivu, profile preview, base dir mobile.
- 2026-07-03: EAS build #1 failed at gradle (JPG icon/splash + RN 0.76.6 drift). Fixed: PNG assets 1024/splash 1284x2778, RN 0.76.9. User rebuilds from expo.dev.
- 2026-07-03: Live debounced search all lists; CSV Download buttons on ~20 tables; fixed owner expense missing branch. Field sweep 39/39, crash sweep clean, e2e 56/56.
- 2026-07-03: Added factory-reset endpoint+UI (keeps super admins/settings, seed guard via demo_data_wiped flag), JSON backend backup polished, PDFs: GST CGST/SGST breakdown, page X of Y, downloaded-by footer. All verified incl. 5-page report.
- 2026-07-03: v2 multibranch: users.extra_branches, pharmacist role, customer GST+type, branch manager, supplier payment terms, user delete (deactivate if history), audit old→new diffs, topbar logo, mobile Admin Users/Branches/Reports screens + branch switcher. Migration applied to Supabase.
- 2026-07-03: Created `main` branch on GitHub at v3 commit 256a4a1 per user request. Workflow now: develop+push claude/rs-group-medical-saas-k2eivu (Railway deploys THIS branch, still repo default) → PR → merge into main as stable mirror. PRs need new commits (zero-diff PR impossible).
- 2026-07-03: v3 discounts+stock popups: promotions table (branch/category/medicine offers with dates+min bill), customer discount_percent, users.max_discount_percent (billing staff default 5 via seed, fallback 10), manager-approval flow on over-limit MANUAL discounts (promo/customer types are pre-approved — exempt), item-wise sale_items.discount, discounts report (group=bill|branch|user|customer|product), invoice PDF gross/taxable/net/paid/balance + "You saved" line, stock_update notifications (data JSON payload) emitted on purchase/transfer-receive/positive adjustment with real-time web popup (dashboard/POS/inventory, selected-branch only) + /stock-updates history page + mobile support. e2e now 74 tests. Gotcha: seed categories are dosage forms (Tablet/Wellness/...), NOT 'Vitamins & Supplements'; e2e must reseed first; billing_staff role now includes billing.discount.
- 2026-07-03: Mobile fix: Billing under "All Branches" sent no branch_id → pos-search empty (server needs concrete branch). BranchBar gained requireBranch prop (billing hides All chip, defaults first branch); customer field now live-searches name OR mobile with tappable suggestions. Full mobile API audit script (scratchpad mobile-audit.mjs) 44/44. APK changes need Expo rebuild to reach phones.
- 2026-07-03: Added LogoTitle header (logo on every mobile page via shared logoHeader in App.js navigators) and docs/USER-MANUAL.md (full owner/staff manual). PR#2 (billing search fixes) merged to main.
- 2026-07-03: /api/manual endpoint renders docs/USER-MANUAL.md as branded PDF (server/src/manual.js md-renderer; header/stampFooter now exported from pdf.js). Buttons: web Settings "Download User Manual", mobile More screen (Linking + getAuthToken). PDF text checks must strip DIGITS too (kerning inserts numbers: "MANU 50 AL").
- 2026-07-03: Mobile BranchBar chips → dropdown (touch field + modal list, full names; requireBranch drops All Branches). User prefers dropdowns over horizontal chips.
- 2026-07-03: v4 Purchase module: medicines.strip_count; /inventory/brands + /inventory/generics master lists; purchase items accept new-medicine (medicine_name+brand+generic+strip_count auto-creates), MM/YYYY or YYYY-MM expiry normalised to month-end; purchase PUT (header) + DELETE (reverses stock, refused if sold); reports brands/generics/lowstock; dashboard widgets expiring 30/60/90, expired, batch summary, brand/generic sales; stock search now also matches brand+generic. Mobile: PurchasesScreen + PurchaseEntryScreen (supplier/brand/generic pickers with add-new, MM/YYYY expiry, strip count, barcode scan, AsyncStorage draft autosave). Web: NewPurchase brand/generic datalists + strip + add-new-medicine row, purchase delete, view shows brand/generic/strip; Inventory modal strip_count. e2e 91 tests. Supabase v4 migration applied.
- 2026-07-03: User asked why APK needs manual EAS click while their other app auto-builds. Cause: no build automation here (only server ci.yml). Added .github/workflows/eas-build.yml — triggers `eas build -p android --profile preview --no-wait` on push to main/dev when mobile/** changes. REQUIRES user to add EXPO_TOKEN repo secret (expo.dev access token). Alternative they can use instead: EAS native GitHub build trigger configured on expo.dev project settings (no token/YAML). Their other app likely uses one of these.
- 2026-07-03: OTA-first release flow. Added expo-updates ~0.26.10, app.json updates.url (u.expo.dev/<projectId>) + runtimeVersion policy "fingerprint", eas.json channels preview/production. Two workflows: eas-update.yml (OTA `eas update --branch preview` on any mobile/** push — JS/asset changes reach installed APKs on next launch) and eas-build.yml (now only on native-affecting paths: package.json, app.json, app.config.js, eas.json, assets/** + manual dispatch). Fingerprint policy = OTA auto-applies only when native unchanged; native change → new fingerprint → rebuild needed (safe, no crash). Both need EXPO_TOKEN secret. First APK after adding expo-updates must be a fresh build to establish the fingerprint before OTA works.
- 2026-07-03: Installed the remaining requested skills as repo-local SKILL.md ports (.claude/skills/): ponytail (DietrichGebert/ponytail — lazy-senior decision ladder, always-on coding), headroom (headroomlabs-ai/headroom — compress large tool output, always-on), graphify (safishamsi/graphify — codebase-as-knowledge-graph, on-demand), ruflo (ruvnet/ruflo — agent meta-harness mindset, on-demand). ponytail is a true prompt-skill (full port like caveman); headroom/graphify/ruflo are BEHAVIORAL ports — their real engines (Rust/Python libs, MCP servers, plugin marketplace, `npx ruflo init`) are NOT installed in this headless env. CLAUDE.md updated to list all as active.
- 2026-07-03: Reinforced auto-invocation in CLAUDE.md — graphify & ruflo now auto-activate on task-shape (many-file/cross-entity → graphify; large/wide/parallel work → ruflo) without the user naming them, matching the always-on caveman/ponytail/headroom. All 6 skills confirmed present in .claude/skills/.
- 2026-07-03: Wired real engines per recommendation. Added project-scoped .mcp.json → ruflo MCP (npx ruflo@latest mcp start; approval-gated, auto-offered in interactive Claude Code — canonical `claude mcp add ruflo` form). Added docs/SKILLS-SETUP.md with verified one-command installs: ponytail=/plugin marketplace add DietrichGebert/ponytail + /plugin install ponytail@ponytail; graphify=pip install graphifyy && graphify install then /graphify .; headroom=pip install "headroom-ai[all]" && headroom mcp install. pip/npx/plugin installs are machine-local (ephemeral in this headless env) so user runs them interactively; behavioral ports remain the working layer meanwhile.
- 2026-07-03: User added EXPO_TOKEN GitHub secret. Bumped mobile app version 1.0.0→1.1.0 to trigger the first tokenized EAS build (establishes the fingerprint baseline required before OTA updates apply). Pipeline now fully automatic: JS push→eas-update OTA, native push→eas-build APK.
- 2026-07-03: Connectivity indicator (web + mobile homepage). Server: lightweight public /api/ping (no DB). Web: useConnectivity.js hook polls /api/ping every 15s (+online/offline events, visibility), tracks status/since/history in localStorage; ConnectionStatus.jsx card on Dashboard (🟢/🔴 + "online/offline for Xh Ym" + expandable period history) and ConnectionDot in topbar. Mobile: useConnectivity.js (AsyncStorage-persisted) + ConnectionCard on HomeScreen with history modal. Per-device log, survives reload/restart. e2e 91/91.
- 2026-07-04: FIXED EAS build+OTA failure. Root cause: hand-added expo-updates was pinned to ~0.26.10 (SDK 51) and expo-asset was missing → `eas update`'s `expo export` crashed with "The required package expo-asset cannot be found", and the native EAS build failed in ~30s on the same bad dep set. Fix: read node_modules/expo/bundledNativeModules.json (authoritative SDK-52 versions) → set expo-updates ~0.27.5, add expo-asset ~11.0.5. Verified locally: npm install clean + `npx expo export --platform android` succeeds (767 modules, no error). Gotcha: `npx expo install --fix` can't run in this sandbox (Expo API blocked by proxy) — use bundledNativeModules.json for correct versions offline. mobile has no committed package-lock.json; workflows use npm install.
- 2026-07-04: Usage & Cost monitor (owner). GET /admin/usage: pg DB size, per-table pg_total_relation_size + % share, base64 blob totals (prescription_file/invoice_file — the hidden storage cost), unbounded log growth (audit_logs/notifications/login_history/sessions), 30d activity, ranked recommendations, provider billing links (Supabase/Railway/EAS). POST /admin/usage/cleanup?target=sessions|login_history|notifications|audit_logs|prescriptions|invoice_files prunes safely (super_admin). Web page web/src/pages/Usage.jsx (/usage, settings.manage) with size bars + one-click cleanup. e2e 96.
- 2026-07-04: Mobile login autosave password. Added expo-secure-store ~14.0.1 (SDK52, native → triggers rebuild; export verified). LoginScreen: "Save password (encrypted)" Switch stores {email,password,branch_code} in OS keychain via SecureStore (NOT plaintext AsyncStorage), prefills on launch, cleared when toggled off; added SHOW/HIDE password. app version 1.1.0→1.2.0.
- 2026-07-04: Restricted Usage & Cost monitor to owner (super_admin) only, per request. Backend GET /admin/usage now 403 for non-super_admin (was any settings.manage → included branch_admin). Web: Protected gained `role` prop; /usage route role="super_admin"; NAV item role-gated (perm:[] + role:'super_admin'); nav filter checks item.role. Cleanup was already super_admin. e2e adds branch-admin-denied test → 97.
- 2026-07-04: Usage & Cost monitor on mobile (owner-only). mobile/src/screens/UsageScreen.js: DB size + per-table % bars, file blobs, recommendations, safe-cleanup rows (POST /admin/usage/cleanup), provider billing links (Linking). Registered in App.js stack; MoreScreen entry gated user.role==='super_admin'. JS-only (no native dep) → ships via OTA, no APK rebuild.
- 2026-07-04: Default login per role. seed.js exports DEFAULT_ACCOUNTS (8: admin@/admin123 branch_admin, manager@/manager123, pharmacist@/pharma123, billing@/billing123, inventory@/inventory123, accounts@/accounts123, delivery@/delivery123, audit@/audit123 auditor) seeded ON CONFLICT DO NOTHING (branch-scoped → branch 1, auditor null); owner@/rsgroup123 super_admin already seeded. Inserted all into LIVE Supabase rpwndipzblvrbcdqzwdb via execute_sql (admin@ pre-existed → UPDATE'd to branch_admin+admin123). USER-MANUAL 1.2 table updated with credentials + change-before-live warning. e2e +9 → 106. Fresh Start wipes non-owner defaults.
- 2026-07-04: Hardened OTA-first policy per user "only if OTA not possible use APK build". eas-build.yml now, after a native-affecting file changes, computes the project's native fingerprint via `npx @expo/fingerprint .` (outputs {hash}) and compares to the last finished Android build's fingerprint from `eas build:list --json` (reads fingerprint.fingerprintHash||fingerprintHash). Builds ONLY if hashes differ (or no prior build / force dispatch); otherwise SKIPS and lets OTA handle it. So version bumps / OTA-config edits / JS-only deps no longer waste an APK build. Safe fallback: empty/unparseable → build. Verified @expo/fingerprint hash extraction locally.
- 2026-07-04: Mobile Billing → "＋ Add new customer" inline. Shown when no customer selected + can(customers.manage). Opens modal (name/phone/address/type/GSTIN/discount%), prefills typed term (name vs phone via regex), POST /customers with branch_id, auto-selects created customer for the bill. JS-only → OTA.
- 2026-07-04: Verified customer name persistence/reuse. Add-new-in-billing (POST /customers) stores name+phone+GST+discount and is searchable by name next time (confirmed id25 "Billing Walk-in Test" returned by /customers?q=). Hardened mobile billing quick-entry: module looksLikePhone(); save() now only sends customer_phone when phone-like (never stores a typed name as a phone), and if a NAME is typed without an attached customer it prompts "Add customer" (stores it) vs "Bill as walk-in". Guard bypassed on approval-retry (approvalCreds) and walkIn. JS-only → OTA.
- 2026-07-04: Web POS customer search by NAME or MOBILE. Added a "🔎 Search customer by name or mobile" box with live suggestion dropdown (name · phone · bills · pts · discount badge · credit due); picking attaches customer_id + fills phone/name; "change" clears. Backend /customers already searched both (name ILIKE OR phone ILIKE). Other surfaces already had it (web Customers "Search name or mobile", mobile Customers + mobile Billing). Web-only change.
- 2026-07-04: Added thermal 80mm receipt format (Jan Aushadhi bill style, requested via reference photos) — new thermalReceiptPdf() in server/src/pdf.js (Courier monospace, SN/Description/Batch/Exp/Amount rows, round-off, "you saved" line), served at GET /sales/:id/pdf?format=thermal. POS BillDoneModal "Print Receipt" now uses this format for counter printing; "PDF Bill" still serves the full A4 tax invoice for records/GST. Verified rendering standalone (no DB) via scratch script — fixed a font-size-inherited dash-line wrap bug before shipping. Pushed to claude/bill-type-generation-m5kud0.
- 2026-07-04: Per follow-up "use thermal for everything, wording must match uploaded bill" — flipped /sales/:id/pdf default to thermal (A4 tax invoice now opt-in via ?format=a4); POS "PDF Bill"/"Print Receipt" and Sales history PDF link all serve thermal now. Rewrote wording to mirror the reference bill exactly: D.L.No./GSTIN/Phone header lines, "Bill No./Bill Date", "Name/Add./Doct" (customers.address, branch.city fallback), plain-number TOTAL/R.OFF, "Total Bill Value is :" + amount spelled in words (added numberToWords() — Indian lakh/crore grouping), "Today you saved", sign-off "Goods Once Sold.../Wishing You a Speedy Recovery/Have a Nice Day, >>>>> Thanking You <<<<<". invoiceCfg.footer still overrides the sign-off first line if set. A4 invoicePdf() kept intact, just no longer the default.
- 2026-07-04: Follow-up "even header/footer wording use it" — matched the reference bill's exact structural details: D.L.NO. label uppercased, totals block wrapped in a +---+ boxed border (was plain dash) like the sample, line order corrected to "Today you saved" before the amount-in-words line. Gotcha: adding the division/tagline header line pushed content past the estimated page height (spilled to a blank page 2) — bumped thermalReceiptPdf's page-height constant 210→245 (+items*9) with margin for optional lines. Always re-render+inspect via Read after touching this layout; the height is a hand-tuned estimate, not measured.
- 2026-07-04: Shipped thermal-receipt feature per "commit merge & PR, you deploy it" — opened PR #25 (claude/bill-type-generation-m5kud0 → main), merged. Gotcha found: Railway's tracked branch claude/rs-group-medical-saas-k2eivu was 24 commits behind main (0 unique commits of its own) — recent merged PRs (#23 customer persistence, #24 POS name/mobile search) had never actually reached production. Fast-forwarded claude/rs-group-medical-saas-k2eivu to main (safe, pure ancestor) and pushed to trigger Railway auto-deploy — this deploy carries those 24 previously-stranded commits too, not just the receipt change. Could not verify the live Railway URL from this sandbox (proxy blocked the outbound curl); user should confirm via Railway dashboard or the app's connectivity indicator.
- 2026-07-04: Mobile BillingScreen "Print Receipt" — the bill-saved Alert now has a button that opens `${BASE_URL}/api/sales/:id/pdf?token=...` via Linking.openURL (same pattern as the User Manual link in MoreScreen.js; auth middleware already accepts ?token= per server/src/auth.js). Same thermal-receipt endpoint the web POS uses. JS-only, ships via OTA. Verified syntax with esbuild (mobile/node_modules isn't installed in this sandbox) since Expo/Metro can't run headless here — not run on-device. Pushed to claude/bill-type-generation-m5kud0, not yet merged/deployed (only the earlier web thermal-receipt work was merged+deployed this session).
- 2026-07-05: Added real branch to LIVE Supabase (rsgroup.branches): RSG-KOL 'Makkal Marunthagam', Kolathur Chennai address/phone/GSTIN 33CHYPS5063G2ZQ/DL TN/Z05/20&21/00513 from the reference bill photo. Set as default by overwriting settings.company (the fallback identity thermalReceiptPdf/invoicePdf use when a branch doesn't override) to the same name/address/phone/gstin/drug_license — no is_default column exists on branches, this is the actual "default store identity" mechanism in this codebase. Deliberately did NOT touch seed.js (demo data RS Medicals Chennai/Madurai/Coimbatore, used by e2e + factory-reset) — gotcha: `node server/src/seed.js --force` will revert company settings/branches to fictional demo data, live edit is not seed-persisted.
- 2026-07-05: Fixed thermal receipt bug from a printed-bill photo: TOTAL/R.OFF/"Total Bill Value is"/"saved"/words-in-rupees lines used align:'right' with no width, so pdfkit right-aligned them to the full page margin (211pt) instead of the item table's actual border width (187.2pt, measured via doc.widthOfString) — made them print past the dashed/boxed border. server/src/pdf.js thermalReceiptPdf now bounds all those lines to the table's real width via a shared alignedRight() helper. Verified the metric with a standalone pdfkit script (no rendering tool available in sandbox to view the output, relied on widthOfString measurement).
- 2026-07-05: Fixed missing expiry visibility at billing time. web POS cart table + mobile BillingScreen cart card didn't show each item's expiry even though it was already fetched (pos-search returns expiry_date, web cart even had an unused `expiry` field). Added Exp column (web) / Exp in subtitle line (mobile, plus wired expiry into the mobile cart item which hadn't been capturing it at all). Confirmed stock-adding already required expiry everywhere (web Purchases type=month input, mobile PurchaseEntryScreen MM/YYYY, both server-validated) — that gap didn't actually exist, just wasn't visible in the cart.
- 2026-07-05: User re-shared reference bill photo + asked for HTML/thermal-print artifact + server template ("all"). Server side already done (thermalReceiptPdf in pdf.js, merged main via PR#25/#26) — no code change needed, branch claude/pharmacy-bill-template-qkz0xm already flush with main. Built printable HTML preview artifact (paper-roll look, print CSS, light/dark chrome) mirroring the exact wording/layout.
- 2026-07-04: Full web-POS parity for mobile BillingScreen (branch claude/mobile-billing-page-parity-3uzpnl, pushed). Added: held bills (list+resume, backed by existing /sales/held + resume_sale_id), doctor name field, prescription capture (reused existing expo-camera CameraView.takePictureAsync base64 — deliberately avoided expo-image-picker to keep this JS-only/OTA, no native rebuild), per-item ₹ discount input on each cart row, manual qty TextInput alongside +/- steppers, full totals breakdown box (Gross/Item Disc/Bill Disc/Taxable/GST incl./Round off/Net Payable) matching web POS's exact math (subtotal-based bill discount, GST-shrinks-with-discount formula). Payment: added credit mode + split-across-methods toggle (cash/upi/card/credit, sum-validated) — was cash/upi/card single-mode only before. Bill-saved flow replaced with a full Modal (itemised list, saved-amount banner, WhatsApp share via /sales/:id/whatsapp, Print/PDF link) instead of a plain Alert. manualDiscount/approval-limit calc updated to include item-wise discounts (was bill-level only), matching server/src/routes/sales.js enforcement. Verified via esbuild parse only (no Expo/Metro in this sandbox) — needs on-device check before trusting the camera-capture flow and RN layout.
